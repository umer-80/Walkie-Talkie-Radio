
# Project Title: P2P Global Radio (Cross-Platform Walkie-Talkie)
# Goal: Build a serverless, peer-to-peer audio streaming application that works via WebRTC for long-distance and Wi-Fi Direct for local use.

# Phase 1: The "Engine" (Web-Based Prototype)
# "Create a single-page web application using Vanilla JavaScript and WebRTC. The app must allow two users to connect directly using a manual 'Signaling' method (No central server).

# Core Logic:

# Handshake: Generate an SDP Offer string that can be shared. Provide an input field to paste an SDP Answer string.

# Audio Stream: Use navigator.mediaDevices.getUserMedia to capture mono audio at 16kHz.

# PTT Mechanism: Implement a 'Push-to-Talk' button. Audio data should only be sent through the RTCPeerConnection while the button is held (onMouseDown/onPointerDown).

# Receiver: Automatically play incoming audio streams using the AudioContext API to minimize lag."

# Phase 2: The "Radio" UI (Branding & Experience)
# "Design a high-fidelity, 'Rugged Radio' themed UI.

# Visual Elements: A large circular PTT button, a digital frequency display (e.g., 'CH-01'), and a signal strength meter.

# Audio Feedback: Add a short 'Chirp' sound effect (MP3) when the PTT button is released (the 'Roger' beep) and a 'Static' white noise floor when the connection is active but no one is talking."

# Phase 3: The Landing Page (Marketing & Distribution)
# "Create a modern, dark-themed landing page.

# Hero Section: Catchy title: 'Global Radio: Zero Servers. Total Privacy.'

# Instructions: >    1. Download the APK.
# 2. Share your Digital Key with a friend.
# 3. Push to talk.

# Download Section: A prominent 'Download for Android' button that links to the .apk file hosted on GitHub or Firebase."

# Phase 4: APK Conversion (Mobile Implementation)
# "Wrap the web application into an Android Project using WebView or Cordova.

# Permissions: Ensure RECORD_AUDIO, INTERNET, and MODIFY_AUDIO_SETTINGS are requested.

# Optimization: Configure the WebView to allow MediaDevices access without user prompt after the first time.

# Offline Logic: Add a secondary toggle for Wi-Fi P2P (Wi-Fi Direct) for when internet is unavailable."



# Component,Technology,Why this choice?
# Web Frontend,HTML5 / Tailwind CSS / Vanilla JS,"Lightweight and fast; perfect for a ""browser-first"" prototype."
# Real-time Audio,WebRTC API,The gold standard for peer-to-peer audio without needing a central server.
# Audio Processing,Web Audio API,"Handles the ""Chirp"" sounds, white noise, and gain control."
# Mobile Wrapper,Apache Cordova or Capacitor,Easily turns your web app into an APK while giving access to native hardware.
# Offline Connectivity,Android Wi-Fi Direct (P2P),Allows phones to talk without any internet or Wi-Fi router.


# # Updated requirements below
# Role: Senior Full-Stack Engineer specializing in WebRTC.

# Objective: Upgrade the current P2P Radio (WebRTC) app to support global connections across different networks and implement a professional QR-code-based signaling system with session persistence.

# Requirement 1: Global Connectivity (ICE/STUN)

# Update the RTCPeerConnection configuration to use Google s public STUN servers (stun:stun.l.google.com:19302).

# Implement an onicecandidate listener that waits for the iceGatheringState to reach 'complete' before generating the final SDP "Digital Key" for the QR code. This ensures the key contains the global public IP, not just the local Wi-Fi IP.

# Requirement 2: QR Code Signaling & Image Import

# Replace manual text pasting with a QR code system using the html5-qrcode or davidshimjs-qrcodejs library.

# Add a "Share Key" button that generates a QR code image and opens the native share menu.

# Add an "Import Key from Gallery" feature that allows a user to upload a screenshot of a QR code, decodes it, and automatically initiates the setRemoteDescription handshake.

# Requirement 3: Connection Persistence & Room Logic

# Implement a "Session Recovery" system using localStorage. Save the successful SDP handshake data so that if the app is refreshed or closed, it attempts to restartIce() and reconnect to the previous peer automatically.

# The connection must remain active until the user explicitly clicks a "Disconnect" or "Leave Room" button.

# Add an iceconnectionstatechange listener to detect drops and show a "Reconnecting..." status overlay on the UI.

# Requirement 4: UI/UX Refinement

# Ensure the PTT (Push-to-Talk) logic is robust: only enable the microphone track during onpointerdown and disable it on onpointerup.

# Add a visual "Signal Strength" meter based on the WebRTC getStats() API to show connection quality.

You are an expert full-stack P2P developer with deep experience in WebRTC, UDP hole-punching, and real-time audio streaming.
I am building a simple project called "P2P Radio" — a digital walkie-talkie / two-way radio that works purely peer-to-peer over ANY network (Wi-Fi, 4G/5G, even different countries).
Core Requirements (must be exactly like this):

Two users only (1-to-1 connection, no groups for now).
Each user creates their own "Channel" (like a radio frequency).
The other user can connect to that channel using a simple code or link.
Once connected, they can talk to each other in real-time (push-to-talk or voice-activated mode).
Voice must travel directly P2P (no central media server for audio — only optional lightweight signaling).
Must work over any network (NAT traversal, firewalls, CGNAT, mobile data, etc.).
Keep everything extremely simple — clean minimal UI, no extra features yet.

Technical Constraints:

Use WebRTC (preferred for web + easy Electron/Capacitor export) OR native libraries if targeting only mobile.
Audio quality: 16kHz or 24kHz mono, low latency (<300ms ideal).
Support push-to-talk (hold button to speak) + optional voice activity detection.
Show connection status: "Connecting...", "Connected", "Disconnected", "Poor connection".
Channel ID should be short and easy to share (6-8 characters or QR code).
Works on browser, Android, iOS, Windows (ideally one codebase).

Deliverables I want from you right now:

Complete project architecture (signaling server + P2P flow diagram in text).
Recommended tech stack (frontend + backend signaling + audio handling).
Step-by-step implementation plan (from zero to working prototype).
Full working code structure (folder layout + key files with comments).
Ready-to-run signaling server code (Node.js + Socket.io or simple WebSocket).
WebRTC connection code with proper ICE/STUN/TURN handling (include free STUN servers).
Audio capture → encoding → transmission → playback pipeline.
Common debugging checklist for "can't connect" or "no audio" issues.

Style: Write everything as if you are talking to someone who knows basic JavaScript but struggles with P2P and WebRTC. Use simple English, lots of comments in code, and explain why every important line exists.
Start by giving me the architecture + tech stack recommendation first, then continue with the full code.
Project name: P2P Radio